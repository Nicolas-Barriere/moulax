# Moulax Docker Compose configuration
# Copy this to .env and adjust ports if they conflict with your machine.

# Host port for PostgreSQL (container always listens on 5432 internally)
DB_PORT=5434

# Host port for the Phoenix backend (container listens on 4000)
BACKEND_PORT=4001

# Host port for the Next.js frontend (container listens on 3000)
WEB_PORT=3005
